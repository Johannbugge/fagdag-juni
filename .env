# Public function URL — committed on purpose. Not a secret; the function is
# deployed with --no-verify-jwt so the browser can call it directly.
# If you need to point at a different host for local testing, copy this file
# to .env.local and override there (.env.local is git-ignored).
VITE_API_URL=https://pbyqwlidgbszldwkfvtt.supabase.co/functions/v1/super-endpoint
