# Optional override. The default URL in `.env` already points at the workshop
# function — you only need this file if you're testing against a local
# `supabase functions serve` or a different deployment.
VITE_API_URL=http://localhost:54321/functions/v1/workshop
